libquo TODO
o Thread protection - Should we do this?
o Expose memory API.
o Maybe at some point provide capability to query #x on some obj:
  like x cores on a particular socket. Primarily for asymmetric topos.
o Install libquo.h and create quo subdirectory with all the other includes (if
  needed).
o Return popped CPU set to caller (API).
o Add library versioning.
o NUMA nodes.
o Fortran 90 demo.
o core, numa node, etc. ranks.
o Use hwloc_get_proc_membind_nodeset for mem stuff?
o hwloc_cpuset_to_nodeset may be useful.
o maybe use hwloc_distribute to distribute processes over the machine.
o auto magic.
