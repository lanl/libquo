libquo TODO
o Fortran interface.
o global placement api
- hwloc_obj_type_snprintf
- hwloc_obj_cpuset_snprintf
o add support query
o auto set library version (api call and so)
o Thread protection - Should we do this?
o Expose memory API.
o Return popped CPU set to caller (API).
o NUMA nodes.
o Fortran 90 demo.
o Use hwloc_get_proc_membind_nodeset for mem stuff?
o hwloc_cpuset_to_nodeset may be useful for NUMA things.
o maybe use hwloc_distribute to distribute processes over the machine.
o auto magic.
